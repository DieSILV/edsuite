import 'package:flutter/material.dart';

class ColorsCustom {
  // ===== DARK THEME COLORS =====
  /// Fondo principal oscuro
  static const Color darkPrimaryBackground = Color(0xFF181A20);

  /// Fondo secundario oscuro (cards, secciones)
  static const Color darkSecondaryBackground = Color(0xFF23262F);

  /// Texto principal claro
  static const Color darkPrimaryText = Color(0xFFF5F6FA);

  /// Texto secundario claro
  static const Color darkSecondaryText = Color(0xFFB0B3C7);

  /// Bordes/divisores oscuros
  static const Color darkBorderDivider = Color(0xFF353945);

  /// Color de error oscuro
  static const Color darkError = Color(0xFFFF5C5C);

  /// Color de advertencia oscuro
  static const Color darkWarning = Color(0xFFFFC542);

  /// Color de éxito oscuro
  static const Color darkSuccess = Color(0xFF34D399);
  // ===== PRIMARY COLORS =====
  /// Primary blue - Primary buttons, headers, links
  static const Color primaryBlue = Color(0xFF2196F3);

  /// Secondary green - Success, secondary buttons, active icons (verde más natural)
  static const Color secondaryGreen = Color(0xFF10B981);

  // ===== HOVER AND ACCENT COLORS =====
  /// Dark blue - Hover, dark blue accents
  static const Color darkBlue = Color(0xFF1976D2);

  /// Dark green - Hover on green buttons or indicators
  static const Color darkGreen = Color(0xFF059669);

  // ===== SOFT COLORS =====
  /// Light blue - Active element backgrounds, chips
  static const Color lightBlue = Color(0xFF64B5F6);

  /// Light green - Tag backgrounds, soft success
  static const Color lightGreen = Color(0xFF6EE7B7);

  // ===== BACKGROUNDS =====
  /// Primary background - General app background
  static const Color primaryBackground = Color(0xFFF9FAFB);

  /// Secondary background - Cards, internal sections
  static const Color secondaryBackground = Color(0xFFEDEDED);

  // ===== TEXT =====
  /// Primary text - Titles, main text
  static const Color primaryText = Color(0xFF1E1E1E);

  /// Secondary text - Subtitles, auxiliary text
  static const Color secondaryText = Color(0xFF666666);

  // ===== BORDERS AND DIVIDERS =====
  /// Border/Divider - Lines, separators
  static const Color borderDivider = Color(0xFFDADADA);

  // ===== STATES AND ALERTS =====
  /// Warning - Pending or incomplete documents
  static const Color warning = Color(0xFFFAD02E);

  /// Error - Download failures, alerts
  static const Color error = Color(0xFFFF4D4F);
}
